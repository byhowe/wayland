[package]
name = "wayland-test"
version = "0.1.0"
edition = "2024"

[dependencies]
wayland-core = { version = "0.1", path = "../wayland-core" }
wayland-client = { version = "0.1", path = "../wayland-client" }
